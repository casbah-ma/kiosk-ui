import Header from "../../Header";

const Home = ({ HeaderProps, SwiperProps, CategoriesProps, FeedBackProps }) => {
  return (
    <>
      <Header {...HeaderProps} />
    </>
  );
};

export default Home;
